# Arbitrage bot budget management (deprecated)

This document is no longer applicable. The arbitrage bot is range-only (price bands + configurable target, default 3%).
See `README.md` and `.env.example` for current configuration.
