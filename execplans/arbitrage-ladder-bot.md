# Deprecated execplan

The arbitrage bot is range-only now (price bands + configurable exit target, default 3%). The
legacy “ladder” execplan has been retired; see `README.md` and
`.env.example` for current behavior and configuration.
